import React from 'react'
import { RouteComponentProps } from '@reach/router'

export const ConfigurationPage = (props: RouteComponentProps) => {
  return <React.Fragment>This page smells like a new car...</React.Fragment>
}
 